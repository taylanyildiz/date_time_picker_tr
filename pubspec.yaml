name: date_time_picker_tr
description: Date Time Picker on desktop tr language.
version: 0.1.4
homepage: https://github.com/taylanyildiz/date_time_picker_tr.git

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

